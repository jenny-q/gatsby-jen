import React from "react"
import PrimaryLayout from '../layouts/PrimaryLayout'

export default () => (
    <PrimaryLayout>
        <h1>About Us</h1>
            <p>Here is some random text as a placeholder for now</p>
    </PrimaryLayout>
)